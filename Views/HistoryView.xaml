<UserControl x:Class="Converto_IT008_WPF.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Converto_IT008_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="History" 
                   Foreground="#F24822"
                   FontWeight="SemiBold"
                   FontSize="40"/>
            <Border Height="400" 
                Background="White" 
                Margin="0,30,0,10"
                Width="760"
                BorderThickness="1"
                HorizontalAlignment="Center"
                BorderBrush="#4F4E4E"
                Padding="0,10,0,0"
                CornerRadius="6">
                <Border.Effect>
                    <DropShadowEffect/>
                </Border.Effect>
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="No." Style="{StaticResource HeaderHistory}"/>
                        <TextBlock Text="Type" Style="{StaticResource HeaderHistory}" Grid.Column="1"/>
                        <TextBlock Text="Time" Style="{StaticResource HeaderHistory}" Grid.Column="2" />
                        <TextBlock Text="Cost" Style="{StaticResource HeaderHistory}" Grid.Column="3"/>

                        <Border Grid.Row="1" Grid.ColumnSpan="4" BorderThickness="0.5" BorderBrush="#4F4E4E" Margin="0,10,0,0"/>
                    </Grid>
                    
                    
                </StackPanel>


            </Border>

            
        </StackPanel>
    </Grid>
</UserControl>
