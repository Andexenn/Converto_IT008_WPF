<Window x:Class="Converto_IT008_WPF.Views.Popups.Terms_ConditionsPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Converto_IT008_WPF.Views.Popups"
        mc:Ignorable="d"
        Height="500" Width="450"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize">

    <Border CornerRadius="5" Background="White" MouseDown="Border_MouseDown" BorderBrush="Black" BorderThickness="1">
        <Grid Margin="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Style="{StaticResource CancelButton}" HorizontalAlignment="Right" Content="x" Margin="0,0,5,0" Command="{Binding ClosePopupCommand}" Cursor="Hand"/>

            <TextBlock  Text="Terms and Conditions" 
                        Grid.Row="1"
                        Foreground="#F24822"
                        FontWeight="SemiBold"
                        HorizontalAlignment="Left"
                        FontSize="28"/>

            <ScrollViewer Grid.Row="2">
                
            </ScrollViewer>

            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Style="{StaticResource CTA}" Background="#F24822" Content="Accecpt" Foreground="White" Padding="10,5,10,5" BorderThickness="0" />
                <Button Style="{StaticResource Signup}" Content="Decline" Grid.Column="2" Margin="0,0,20,0" VerticalAlignment="Center"/>

            </Grid>
        </Grid>
    </Border>
</Window>
