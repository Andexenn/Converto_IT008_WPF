<UserControl x:Class="Converto_IT008_WPF.Views.ServiceViews.MarkdownToTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:md="clr-namespace:MdXaml;assembly=MdXaml" 
             
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#FFFBFBFB">
        <StackPanel Margin="20">
            <TextBlock Text="Markdown to RichText" 
                       Foreground="#F24822" FontWeight="SemiBold" FontSize="40"/>
            <TextBlock Text="Render markdown syntax to beautiful rich text." 
                       FontSize="16" FontStyle="Italic" Foreground="Gray" Margin="0,5,0,20"/>

            <Grid Height="450">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <GroupBox Header="Input (Markdown)" Grid.Column="0" FontSize="14" FontWeight="Bold">
                    <TextBox x:Name="txtInput" 
                             AcceptsReturn="True" TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto"
                             Padding="10" FontWeight="Normal" FontSize="14"
                             BorderThickness="0" 
                             TextChanged="TxtInput_TextChanged"
                             FontFamily="Consolas"/>
                </GroupBox>

                <GroupBox Header="Rich Text Output" Grid.Column="2" FontSize="14" FontWeight="Bold">

                    <md:MarkdownScrollViewer x:Name="mdViewer"
                             BorderThickness="0"
                             Padding="10"
                             VerticalScrollBarVisibility="Auto"
                             FontWeight="Normal" 
                             MarkdownStyleName="GithubLike"/>
                </GroupBox>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>